fastapi
uvicorn
python-multipart
opencv-contrib-python  # dnn_superres(고화질 확대) 포함 — plain opencv-python 대신 이걸 설치
numpy

# LaMa 엔진(선택): torch가 있고 big-lama.pt(약 205MB)가 있으면 SOTA 품질로 추론.
# 모델: https://github.com/enesmsahin/simple-lama-inpainting/releases/download/v0.1.0/big-lama.pt
# 없으면 자동으로 OpenCV Telea 폴백으로 동작한다.
torch

# 사람 전체 지우기 — DeepLabV3 (첫 호출 시 가중치 161MB 자동 다운로드)
torchvision

# 텍스트 개인정보 탐지 — EasyOCR (첫 호출 시 인식 모델 자동 다운로드)
easyocr

# 얼굴 복원 — GFPGAN 1.4 ONNX (gfpgan_1.4.onnx 340MB 수동 다운로드 필요)
# 모델: https://huggingface.co/facefusion/models-3.0.0/resolve/main/gfpgan_1.4.onnx
# 번호판 탐지 — YOLO11s ONNX (plate_yolo11s.onnx 38MB, 없으면 캐스케이드 폴백)
# 모델: https://huggingface.co/morsetechlab/yolov11-license-plate-detection/resolve/main/license-plate-finetune-v1s.onnx
onnxruntime
